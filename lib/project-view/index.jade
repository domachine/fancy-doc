!!!5
html
  head
    title= project
    link(rel='stylesheet', href='/app.css')
  body
    #modules
      .project
        a(href='/projects/#{project.name}')
          = project.name
      each ms, path in modules
        span= path
        ul
          each module in ms
            li
              a(href='/projects/#{project.name}/docs/#{module.path}/#{module.name}')
                = module.name
    #readme
      div
        if readme == null
          h1 No readme found!
        else
          != readme
